/* =========================================================
   config.js
   共通設定
========================================================= */

const CONFIG = {
    previewWidth:390,
    previewHeight:600,
    defaultFrame:"f1"
};

/* 各フレームモジュールを登録する共通レジストリ */

const FRAME_REGISTRY = Object.create(null);
